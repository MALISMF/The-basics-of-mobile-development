<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="item"
            type="com.example.weatherworkmanager.model.CityWeatherItem" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="10dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="@android:color/white">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- Название города -->
            <TextView
                android:id="@+id/tvCity"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="@{item.titleFormatted}"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#1A237E"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@id/tvTemp"
                tools:text="Moscow, RU" />

            <!-- Температура — крупно справа -->
            <TextView
                android:id="@+id/tvTemp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{item.tempFormatted}"
                android:textSize="28sp"
                android:textStyle="bold"
                android:textColor="#E65100"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="–3.5°C" />

            <!-- Описание погоды -->
            <TextView
                android:id="@+id/tvDescription"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="@{item.description}"
                android:textSize="15sp"
                android:textColor="#546E7A"
                app:layout_constraintTop_toBottomOf="@id/tvCity"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@id/tvFeelsLike"
                tools:text="Небольшой снег" />

            <!-- Ощущается как -->
            <TextView
                android:id="@+id/tvFeelsLike"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="@{item.feelsLikeFormatted}"
                android:textSize="12sp"
                android:textColor="#78909C"
                app:layout_constraintTop_toBottomOf="@id/tvTemp"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="Ощущается: –7.0°C" />

            <!-- Разделитель -->
            <View
                android:id="@+id/divider"
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_marginTop="12dp"
                android:background="#E0E0E0"
                app:layout_constraintTop_toBottomOf="@id/tvDescription"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- Влажность -->
            <TextView
                android:id="@+id/tvHumidity"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:drawablePadding="4dp"
                android:text="@{item.humidityFormatted}"
                android:textSize="13sp"
                android:textColor="#455A64"
                app:layout_constraintTop_toBottomOf="@id/divider"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@id/tvWind"
                tools:text="Влажность: 83%" />

            <!-- Ветер -->
            <TextView
                android:id="@+id/tvWind"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:text="@{item.windFormatted}"
                android:textSize="13sp"
                android:textColor="#455A64"
                app:layout_constraintTop_toBottomOf="@id/divider"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="Ветер: 4.5 м/с" />

            <!-- Статус загрузки -->
            <ProgressBar
                android:id="@+id/itemProgress"
                style="?android:attr/progressBarStyle"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginTop="8dp"
                android:visibility="@{item.loading ? android.view.View.VISIBLE : android.view.View.GONE}"
                app:layout_constraintTop_toBottomOf="@id/tvHumidity"
                app:layout_constraintStart_toStartOf="parent" />

            <!-- Сообщение об ошибке -->
            <TextView
                android:id="@+id/tvError"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@{item.error}"
                android:textColor="#C62828"
                android:textSize="13sp"
                android:visibility="@{item.error != null ? android.view.View.VISIBLE : android.view.View.GONE}"
                app:layout_constraintTop_toBottomOf="@id/tvHumidity"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="Ошибка сети" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>
</layout>
